---
interface Props { items: { name: string; path: string }[] }
const { items } = Astro.props;
---
<nav class="crumbs" aria-label="Breadcrumb">
  <div class="wrap">
    <ol>
      {items.map((item, i) => (
        <li>
          {i < items.length - 1
            ? <a href={item.path}>{item.name}</a>
            : <span aria-current="page">{item.name}</span>}
        </li>
      ))}
    </ol>
  </div>
</nav>
<style>
  .crumbs { border-bottom: 1px solid var(--ink-12); background: var(--bone); }
  .crumbs ol {
    display: flex; flex-wrap: wrap; gap: .5rem; list-style: none;
    margin: 0; padding: .85rem 0; font-size: var(--step--1); color: var(--ink-70);
  }
  .crumbs li + li::before { content: "/"; margin-right: .5rem; color: var(--ink-muted); }
  .crumbs a { text-decoration: none; }
  .crumbs a:hover { text-decoration: underline; text-decoration-color: var(--clay); }
  .crumbs [aria-current="page"] { color: var(--ink); font-weight: 500; }
</style>
