{
  "name": "frontporchco",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Front Porch Collective LLC \u2014 the company website. Staged, not deployed.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "audit": "node scripts/audit.mjs",
    "contrast": "node scripts/contrast.mjs",
    "check": "npm run build && npm run audit && npm run contrast"
  },
  "dependencies": {
    "astro": "^7.2.4"
  }
}
